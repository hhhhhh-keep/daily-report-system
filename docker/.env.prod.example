# 仅用于 docker-compose.yml：正式环境。填入正式凭据后保存为未提交的 .env.prod。
POSTGRES_USER=daily_report
POSTGRES_PASSWORD=replace-with-a-strong-production-database-password
ADMIN_USERNAME=admin
ADMIN_INITIAL_PASSWORD=replace-with-a-strong-production-admin-password
LLM_ENDPOINT=
LLM_MODEL=
LLM_API_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_HEALTH_ENABLED=false
WEB_BASE_URL=https://replace-with-production-domain
HTTP_PORT=8080
APP_TIMEZONE=Asia/Shanghai
